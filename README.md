# Sprint4Project
# Use app.py file to launch streamlit app
# Refer to notebooks/EDA.ipynb for exploritory analysis of "vehicles_us"
# To Run on render, open your account on render.com, create a new web service linked to your GitHb repoitory. Configure the new Render web service. To Build Command, add: pip install streamlit & pip install -r requirements.txt . To Start Command, add: streamlit run app.py. Deploy to Render, wait for build to succeed. Verify app is successful at URL: https://<APP_NAME>.onrender.com/